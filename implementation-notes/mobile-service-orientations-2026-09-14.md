# Restore orientation within each mobile offering

Joe clarified that the two-service choice does not replace orientation within
the selected offering, and explicitly requested reusing the former AI display.

Reactivated the existing appendPathDoor renderer and its numbered architectural
path illustration. AI retains the original five labels and situation list;
Thinking Better Together uses the same display with its three situations.
Selecting or switching offerings lands on Overview before exhibit 1. Overview
has a progress control and is present in both directions of the circuit.

Validation: npm test, mobile DOM interactions (3/5 entries, numbered AI
illustration, selection landing, swipe to first exhibit, overview return,
switching, elevator, inspector), desktop service-layout regressions, and
whitespace checks pass. No rendered browser verification was performed.

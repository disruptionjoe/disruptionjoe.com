# Compact Work With Joe choice area

Joe reported an oversized empty choice room, awkward sign spacing and oversized
hallway-like door frames. The desktop choice footprint is now 6 × 10 units
(previously 11 × 12); the Orientation approach is 0.35 units rather than 3.85.
Two three-unit-wide L-shaped passages connect to the existing service rooms.

The choice sign is centered between the actual openings, reduced to 2.6 × 1.6
units, and uses concise two-column copy. Side statements fit the shorter walls.
Entrance frames match actual three-unit openings, with lower frames/signs and
3.6-unit corridor ceilings. Generic portals retain their existing defaults.

Both service rooms still lead into Methods and Tools, then Who Is Joe. Existing
mobile/shared content and downstream routing are unchanged. This desktop batch
was isolated from unrelated uncommitted mobile edits in the original checkout.

Validation: syntax suite, production-coordinate forward/reverse routes, doorway
clearances, removed-floor checks, compact-footprint and short-approach checks;
shared pre-initMuseum source comparison and diff whitespace. Browser rendering
was not inspected because the earlier denied domain permission remains in force.
These are source checks, not a claim of visual acceptance.

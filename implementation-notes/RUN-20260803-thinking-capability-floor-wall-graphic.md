# RUN-20260803 Capability-Floor Wall Graphic

## Bottom line

Work With Joe now includes a large generated architectural infographic that
makes the Identify → Establish → Raise capability progression tangible.

## Generated asset

- Final asset: `assets/thinking/raise-the-floor-capability-path.png`
- Generator: built-in ImageGen
- Dimensions: `1024 × 1536`
- Format: portrait PNG
- Style references: the existing Capability Acceleration, Enablement
  Architecture, and Enhanced Facilitation wall graphics.

## Final prompt

Create a portrait 4:5 wall exhibit in the Disruption Joe black architectural
blueprint style: warm tan, camel, amber, and soft-gold dimensional linework on
near-black. Show a dramatic three-level architectural cutaway that rises from
`1 IDENTIFY / CURRENT CAPABILITY + FRICTION`, through
`2 ESTABLISH / MEASURABLE, REPEATABLE WAYS OF WORKING`, to
`3 RAISE / CONNECT WORKFLOWS + SCALE`. Show
`TEAM → MULTIPLE TEAMS → BUSINESS UNIT → ORGANIZATION` as the scale path.
Represent `LEADERSHIP ADVISORY` as a structural brace touching all three
floors. Represent `HIGH-PERFORMANCE ACCELERATION` as a separate energy path
that rises beyond the upper floor toward `RAISE THE CEILING`. Title the piece
`RAISE THE FLOOR`. Use legible condensed uppercase typography, strong negative
space, cinematic restraint, and precise dimensional geometry. Render each
required label exactly once. Avoid blue neon, SaaS gradients, robots, brains,
generic AI icons, fake dashboards, clutter, duplicate text, and watermarks.

## Placement

- Mounted on the empty southern segment of the Work With Joe east wall,
  immediately right of the Who Is Joe hallway when facing that hallway.
- Centered at local `z=-9.0` with a `2.85 × 4.275` scene-unit image plane.
- The image leaves `0.575` scene units of lateral clearance at each wall end
  and remains inside the room's vertical wall bounds.
- Added a black backing, restrained gold frame, and local warm light matching
  the existing wall-graphic treatment.
- Preserved the hallway opening, exhibits, movement zones, interactions, and
  mobile behavior.

## Validation

- Confirmed the project asset is `1024 × 1536` and matches the `2:3` display
  plane aspect ratio.
- Confirmed the graphic remains within the solid wall segment and clear of the
  Who Is Joe opening.
- Confirmed existing exhibit placement definitions are unchanged.
- Confirmed the local WebGL experience requests the generated PNG successfully
  and renders without browser console warnings or errors.
- `npm test`
- `git diff --check`
